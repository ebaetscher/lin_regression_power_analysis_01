python script to quantify the probability of detecting a true linear effect between one (or more) predictor variables and one outcome variable,
given a sample size (n), true effect size (R-squared), and number of predictors (default = 1) 
